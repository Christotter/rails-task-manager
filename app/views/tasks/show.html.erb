<%# <h1>My tasks</h1> %>
<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<p>Completed :<%= check_box_tag :completed , 1 , @task.completed ? true : false %></p>

<%= link_to "🛑 #{@task.title}",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

<br>
<%= link_to 'Edit', edit_path %>
<br>
<%= link_to 'Home', index_path %>
